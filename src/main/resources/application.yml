spring:
  main:
    web-application-type: reactive
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: qwen3.5:0.8b
          temperature: 0.7
      embedding:
        options:
          model: nomic-embed-text
logging:
  level:
    org:
      springframework:
        ai: DEBUG
        web:
          client:
            RestClient: DEBUG